Small, framework-agnostic helper classes that don't belong to any other
layer. Kept deliberately minimal, not a dumping ground. Still empty -
nothing built so far has needed it (PromptBuilder, the one candidate,
ended up in app/Services instead - see docs/architecture.md).
