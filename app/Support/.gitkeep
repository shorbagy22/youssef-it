Small, framework-agnostic helper classes that don't belong to any other
layer (e.g. PromptBuilder - pure string composition, no I/O, no external
system). Kept deliberately minimal, not a dumping ground. Populated
starting with the SharePoint/Ollama integration milestone.
